public class Initialization {
    public static void main(String[] args) {
        int x = 5;
        int y = 6;
        int z = 0;

        double decimal; // declaration (tells java what type decimal is)
        decimal = 1; // initialization (tells java that we have a value and are ready to use it)

        System.err.println(z);
    }
}